
* 传统RAG ：查询 → 向量化 → 检索 Top-K → 拼 Prompt → LLM 生成
* Agentic RAG  ：查询 → 主代理 → Sub-Agent检索 → 主代理整合 → LLM 生成
``` markdown
用户请求
   ↓
[Orchestrator Agent / 主代理] ← "这个任务需要查资料"
   ↓
[RAG Sub-Agent] ──→ 检索工具 A
      ↓            检索工具 B
   生成草稿       检索工具 C
      ↓
[主代理审核/整合]
   ↓
最终回答
```